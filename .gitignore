# ═══════════════════════════════════════════════════════════════════
# Trix Audio Converter — .gitignore
# Developer: João Vitor de Melo | joaovmelo259@gmail.com
# https://github.com/damonio13/Trix-Audio-Converter
# ═══════════════════════════════════════════════════════════════════

# ── Rust ──
target/
Cargo.lock

# ── Node / Frontend ──
node_modules/
dist/
build/
.vite/
*.tsbuildinfo

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo
*~
*.sublime-project
*.sublime-workspace
.cursorrules

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db
Desktop.ini

# ── Environment Variables (SECRETOS) ──
.env
.env.*
!.env.example
.env.local
.env.development.local
.env.test.local
.env.production.local

# ── Logs & Debug Runtime ──
*.log
trix_*.txt
trix_debug_log.txt
trix_webview_error.txt
trix_stdout.txt
trix_stderr.txt
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── Temporários ──
temp/
tmp/
*.tmp
*.bak

# ── Build Artifacts ──
*.exe
*.dll
*.so
*.dylib
*.o
*.a
*.lib

# ── NSIS Installer Output ──
installer/output/
TrixAudioConverter-Setup.exe

# ── Cache ──
.cache/
.eslintcache

# ── Secrets & Tokens ──
*.pem
*.key
*.cert
token.json
secrets.json

# ── Dados do Usuário (não commitar) ──
src-rs/data/
data/
queue_state.json
settings.json

# ── WebView cache ──
trix_webview_*/
